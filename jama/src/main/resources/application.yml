spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jama
    username: postgres
    password: ArarTeclado23
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    show-sql: true
